{% extends "LeaptAdminBundle::base.html.twig" %}

{# body block #}
{% block body %}

    {# pageheader block #}
    {% block pageheader %}
        <div class="page-header">
            <h2>{{ "dashboard.title"|trans({}, 'LeaptAdminBundle') }}</h2>
        </div>
    {% endblock pageheader %}

    {# intro block #}
    {% block intro %}
        <p>{{ "dashboard.introduction"|trans({}, 'LeaptAdminBundle')|raw }}</p>
    {% endblock intro %}

    {# widgets block #}
    {% block widgets %}

        {# {% render 'LeaptAdminBundle:Log:list' %} #}

    {% endblock widgets %}

{% endblock body %}